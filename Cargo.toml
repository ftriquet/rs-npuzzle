[package]
name = "npuzzle"
version = "0.1.0"
authors = ["François Triquet <francois.triquet@recast.ai>"]

[features]
default = ["greedy"]

greedy = []

uniform = []

[dependencies]
clap = "2.19.0"
rand = "0.3"
